<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Orchard</title>
    <meta name="description" content="Orchard: fresh fruits, local produce, and more.">

    <!-- favicon -->
    <link rel="icon" href="https://www.orchard.com.au/wp-content/uploads/2024/01/favicon.ico" type="image/x-icon">

    <!-- fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <!-- CSS -->
    <link rel="stylesheet" href="assets/css/styles.css">
  </head>
  
  <body>
    <main id="main" role="main">
      <div class="main__container">
        {{#> layout}}

          <!-- gallery section -->
          <section class="gallery" aria-labelledby="gallery-heading">
            {{> gallery}}
            {{> gallery-modal}}
            {{> content-block}}
          </section>

          <!-- cards section -->
          <section class="cards" aria-labelledby="cards-heading">
            <div class="align-center">
              <h2 id="cards-heading">{{cards.headline}}</h2>
            </div>
            <ul class="cards__container" role="list">
              {{> card cards}}
            </ul>
          </section>

        {{/layout}}
      </div>
    </main>

    <!-- main.js handles gallery and card interactions-->
    <script src="assets/js/main.js" defer></script>
  </body>
</html>
